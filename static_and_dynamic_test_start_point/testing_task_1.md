### Testing task 1:

# Carry out static testing on the code below.
# Comment on any errors that you see below.
# There are 13 errors in total. 

```python

class CardGame:


  def checkforAce(self, card):
    # comparison operator should be == (= is assignment operator)
    if card.value = 1:
      #boolean value need capitalised
      return true
    # missing colon after else keyword
    else
    #boolean value needs capitalised
      return false

  #spelling error keyword should be 'def' not 'dif'
  # missing colon after parameter brackets
  # missing comma between function parameters
  dif highest_card(self, card1 card2)
    # missing colon after if statement condition
    if card1.value > card2.value
      # card not defined - should be card1
      return card
      # missing colon after else keyword
    else
      return card2
 
# missing self parameter
 def cards_total(cards):
   # total variable not initialised properly
   total
   for card in cards:
     total += card.value
     # indentation error - return statement should be outside for loop
     # cannot add string to int value
     return "You have a total of" + total


```
